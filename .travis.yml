language: python
services: mongodb
python:
    - "2.7"
    - "3.3"
install:
  - pip install -r requirements.txt --use-mirrors
script:
  - pytest
branches:
  only:
    - dev
notifications:
  email: false
